import { mdiHomeOutline } from '@mdi/js'

// Sidebar entries. `jfast new view` inserts generated modules at the marker
// below and adds any icon import it needs.
//
// Line comments, not a block comment: the marker ends with */, which would
// close a surrounding block comment early.
export default [
  {
    to: '/',
    icon: mdiHomeOutline,
    label: 'Home',
  },
  /*nuevoModulo*/
]
