import { NavLink } from 'react-router-dom'

import BaseIcon from '@/components/BaseIcon.jsx'
import menu from '@/menuAside.js'

export default function LayoutAuthenticated({ children }) {
  const appName = import.meta.env.VITE_APP_NAME

  return (
    <div className="flex min-h-full bg-slate-50 text-slate-900 dark:bg-slate-900 dark:text-slate-100">
      <aside className="hidden w-64 shrink-0 border-r border-slate-200 bg-white md:block dark:border-slate-700 dark:bg-slate-800">
        <div className="px-6 py-5 text-lg font-semibold">{appName}</div>
        <nav className="px-3">
          {menu.map((item) => (
            <NavLink
              key={item.to}
              to={item.to}
              className={({ isActive }) =>
                `mb-1 flex items-center gap-3 rounded-md px-3 py-2 text-sm hover:bg-slate-100 dark:hover:bg-slate-700 ${
                  isActive ? 'bg-brand-50 text-brand-700 dark:bg-slate-700' : ''
                }`
              }
            >
              {item.icon ? <BaseIcon path={item.icon} size="sm" /> : null}
              <span>{item.label}</span>
            </NavLink>
          ))}
        </nav>
      </aside>

      <div className="flex min-w-0 flex-1 flex-col">
        <header className="flex items-center gap-4 border-b border-slate-200 bg-white px-6 py-4 md:hidden dark:border-slate-700 dark:bg-slate-800">
          <span className="font-semibold">{appName}</span>
        </header>
        <main className="min-w-0 flex-1">{children}</main>
      </div>
    </div>
  )
}
