import BaseIcon from '@/components/BaseIcon.jsx'

export default function SectionTitleLineWithButton({ title, icon, main = false, children }) {
  const Heading = main ? 'h1' : 'h2'
  return (
    <div className="mb-6 flex items-center justify-between gap-4">
      <div className="flex items-center gap-3">
        {icon ? <BaseIcon path={icon} size="lg" className="text-brand-600" /> : null}
        <Heading
          className={`font-semibold tracking-tight text-ink ${main ? 'text-2xl' : 'text-lg'}`}
        >
          {title}
        </Heading>
      </div>
      {children}
    </div>
  )
}
