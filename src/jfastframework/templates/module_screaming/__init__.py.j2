"""Domain modules.

This file makes the modules directory a real package. Without it, pytest can
import a module under two different names (``order`` and ``modules.order``),
which executes the SQLAlchemy model definitions twice and fails with
"Table is already defined for this MetaData instance".
"""
