# Generated from the framework version that scaffolded this service. While
# the framework is a pre-release the pin is exact, because the API moves;
# once it is stable this becomes a compatible-release pin.
jfastframework[{{ extras }}]{{ framework_pin }}
