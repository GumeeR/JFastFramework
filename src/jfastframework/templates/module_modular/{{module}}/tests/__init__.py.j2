"""Tests for the {{ module_title }} module.

Kept as a package so pytest resolves this file as
``modules.{{ module }}.tests.test_{{ module }}`` -- the same package path the
tests themselves import from. Drop this file and the module gets imported
twice under two names.
"""
