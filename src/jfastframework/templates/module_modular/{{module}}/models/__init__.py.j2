"""Models for the {{ module_title }} module.

Two kinds of model, one import path. The entity is what the database stores;
the Pydantic models are what the wire carries. They are separate files because
they change for different reasons and at different speeds.
"""

from __future__ import annotations

from .{{ module }}_entity import {{ Module }}
from .{{ module }}_models import (
    {{ Module }}Base,
    {{ Module }}Create,
    {{ Module }}Read,
    {{ Module }}Update,
)

__all__ = [
    "{{ Module }}",
    "{{ Module }}Base",
    "{{ Module }}Create",
    "{{ Module }}Read",
    "{{ Module }}Update",
]
