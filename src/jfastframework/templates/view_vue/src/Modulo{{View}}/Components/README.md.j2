# {{ view_title }} components

Components used only by this module. Anything reused across modules belongs in
`src/components/`, not here.

| Folder | Holds |
| --- | --- |
| `Modals/` | Dialogs owned by this module |
| `Tables/` | Table and row components for this module's data |

A component that grows a second consumer outside this module has outgrown the
folder — move it up rather than importing across modules.
