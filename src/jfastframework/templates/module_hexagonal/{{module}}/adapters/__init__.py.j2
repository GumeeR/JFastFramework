"""Driving adapters for {{ module_title }}: what calls the use cases.

``http.py`` today. A CLI command, a queue consumer or a gRPC servicer would be
siblings, each translating its own protocol into the same use-case calls and
holding no rules of its own.
"""
