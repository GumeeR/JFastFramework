# {{ service_title }}

API gateway for the **{{ workspace_name }}** workspace.

## Why this exists

One backend needs no gateway — adding one buys a network hop and an outage
surface for nothing. From two onward, every client has to know every hostname,
and that is the problem this solves.

## Routes

| Prefix | Target |
| --- | --- |
{% for route in routes %}
| `{{ route.prefix }}` | `{{ route.target }}` |
{% endfor %}

`GET /gateway/routes` returns the same table at runtime.

## Run

```bash
pip install "jfastframework[server,metrics,gateway]"
cp .env.example .env
uvicorn main:app --reload --port {{ port }}
```

## Regenerate after adding a service

```bash
jfast new service reporting          # registers itself in the workspace
jfast workspace gateway --force      # rewrites the routes above
```

Do not hand-edit `[[plugin.gateway.routes]]`. The workspace file is the source
of truth; this file is derived from it.

## What it does per request

- Forwards method, query, headers and body to the matching upstream.
- Strips hop-by-hop headers, which are about one connection and must not cross.
- Propagates `X-Request-ID` so a trace survives the hop, and sets
  `X-Forwarded-Host` / `X-Forwarded-Proto`.
- Maps an unreachable upstream to `502` and a slow one to `504`, both as
  `application/problem+json`.

## What it deliberately does not do

- **No catch-all route.** Only the configured prefixes are proxied, so
  `/health`, `/ready` and `/metrics` still belong to the gateway itself.
- **No upstream probing in `/ready`.** A gateway whose upstream is restarting
  is still doing its job; cascading readiness failures take the whole system
  out at once.
- **No business logic.** Auth, rate limiting and caching are plugins you enable
  here. Domain code is not.

## Rate limiting

The gateway is where a service-wide limit belongs, because every proxied request
passes through it:

```toml
[plugins]
enabled = ["observability", "cache", "gateway", "ratelimit"]

[plugin.ratelimit]
limit = 100
window = 60.0
```

Token bucket in Redis, keyed on the authenticated subject when there is one and
the client address when there is not, `429` with `Retry-After` and a problem
document when a bucket runs dry. Override it per route with
`Depends(rate_limit(10, 60))`. It needs the `cache` plugin for the Redis
connection, and it does not replace an edge limiter — see
[docs/ratelimit.md](../../docs/ratelimit.md).

Note the boundary: every proxied prefix is one route here, and they all share the
default bucket, so a caller gets one budget across the whole gateway rather than
one per upstream endpoint. A limit that has to know what an individual upstream
endpoint costs belongs in that upstream service.
